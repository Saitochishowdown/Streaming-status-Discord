var Discord = require("discord.js")
client = new Discord.Client()
client.login ("token")
  client.on('ready', () => {
   client.user.SetPresence({ game { name 'Exodius', type "streaming", url https://twitch.tv/Ninja" } });
});
